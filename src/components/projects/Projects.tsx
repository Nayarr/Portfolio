/**
 * Ecran 02, Projets. Pellicule centree (projet actif au centre), puis transition,
 * ecran deploye, ecran explore, fiche detaillee.
 * TODO(feat/projects) : implementer le contenu et les mecaniques.
 */
export function Projects() {
  return <section id="projets" aria-label="Projets" />;
}
