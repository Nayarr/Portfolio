/**
 * Ecran 04, A propos. Photo, recit, formation, centres d'interet, hors-code.
 * TODO(feat/about) : implementer le contenu.
 */
export function About() {
  return <section id="a-propos" aria-label="A propos" />;
}
