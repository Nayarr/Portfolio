/**
 * Ecran 05, Contact. Coordonnees, liens, telechargement du CV.
 * TODO(feat/contact) : implementer le contenu.
 */
export function Contact() {
  return <section id="contact" aria-label="Contact" />;
}
