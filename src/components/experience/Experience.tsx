/**
 * Ecran 03, Experience. Circuit electrique a cabler, revelation des etapes
 * Freelance, ASSURFAST, BUT, Lycee.
 * TODO(feat/experience-circuit) : implementer le contenu et la mecanique.
 */
export function Experience() {
  return <section id="experience" aria-label="Experience" />;
}
